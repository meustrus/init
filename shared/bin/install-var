#!/bin/sh

source "${INIT_REPO:-$(dirname "$0")/../..}/vars.sh"

append "$1=\"$2\" export $1" "$HOME/.profile"
append "$1=\"$2\" export $1" "$HOME/.bash_profile"
append "$1=\"$2\" export $1" "$HOME/.zshenv"

`$1="$2" export $1`
